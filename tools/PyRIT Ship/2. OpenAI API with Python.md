
---

# 🧪 Reproducible Lab Guide: 

## 1️⃣ What This Guide Covers

- How to create and manage your OpenAI API key
- How billing and trial credits work
- How to access GPT models (3.5, 4, 4o)
- How to run a basic Python chat script
- How to avoid common errors (quota, rate limits)

---

## 2️⃣ Step-by-Step Setup

### ✅ Create Your OpenAI Account and API Key

1. Go to [platform.openai.com](https://platform.openai.com/)
2. Sign in and visit [API Keys](https://platform.openai.com/account/api-keys)
3. Click **“Create new secret key”**
4. Save the key in a `.env` file:

```env
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_MODEL_NAME=gpt-3.5-turbo
```

---

### ✅ Install Required Python Packages

```bash
pip install openai python-dotenv
```

---

### ✅ Create Your Python Script (`main.py`)

```python
import os
from openai import OpenAI
from dotenv import load_dotenv

load_dotenv()  # Load .env variables

client = OpenAI(api_key=os.getenv("OPENAI_API_KEY"))

response = client.responses.create(
    model="gpt-4o",
    input="Write a short bedtime story about a Gryph.",
    max_output_tokens=500,
    temperature=0.7,
    top_p=1.0,
)

print(response.output_text)

```

---

## 3️⃣ Billing and Access Tips

|Type|Description|
|---|---|
|**Trial credits**|Free credits expire after 3 months|
|**Prepaid credits**|Valid for 12 months from purchase|
|**ChatGPT Plus ($20)**|Web-only access to GPT-4/GPT-4o — no API credits|
|**API usage**|Billed per token — separate from ChatGPT subscription|

🔍 Monitor usage at [platform.openai.com/usage](https://platform.openai.com/usage)

---

## 4️⃣ Common Errors and Fixes

|Error|Cause|Fix|
|---|---|---|
|`RateLimitError 429`|Too many requests or no quota|Add credits or reduce frequency|
|`insufficient_quota`|No balance or model not enabled|Add funds or switch to `gpt-3.5-turbo`|
|`responses.create()` fails|Wrong method for GPT-3.5|Use `chat.completions.create()` instead|

---

## 5️⃣ Recommended Folder Structure

```
/openai_lab/
├── .env
├── main.py
├── requirements.txt
└── README.md
```
