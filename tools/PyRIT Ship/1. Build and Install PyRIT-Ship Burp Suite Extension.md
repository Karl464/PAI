
Based on the official documentation: [PyRIT-Ship burp_extensionGuide](https://github.com/microsoft/PyRIT-Ship/blob/main/docs/burp_extension.md)

## ✅ Prerequisites

### 1. Java JDK 21

- Download from: https://jdk.java.net/java-se-ri/21
- Extract to a clear folder, e.g.: `C:\Java\jdk-21`
- Add this path to your system environment variables (`PATH`): `C:\Java\jdk-21\bin`

### 2. Gradle 8.5

- Download from: https://gradle.org/releases/
- Extract to: `C:\Gradle\gradle-8.5`
- Add this path to your system `PATH`: `C:\Gradle\gradle-8.5\bin`


### Verify the **PATH** environment variables 

``` Powershell
# Split PATH into individual directories 
$Env:PATH -split ';' | Select-String -Pattern "bin"
```

## 📦 Clone the PyRIT-Ship Repository

Open PowerShell or your terminal and run:

``` Powershell
git clone https://github.com/microsoft/PyRIT-Ship.git
```

## 🧱 Build the Burp Extension

### 1. Navigate to the Java project directory

``` powershell
cd .\PyRIT-Ship\burp_extension\pyritship
```

This folder should contain the file: 
   
- `settings.gradle` → should include:

```
rootProject.name = 'pyritship'
```


### 2. Run Gradle to build the extension

#### Option A: Use the Gradle wrapper (`gradlew.bat`) if available

```powershell
.\gradlew.bat build
```

#### Option B: Use your globally installed Gradle

``` powershell
gradle build
```

This will generate a `.jar` file in:

```
PyRIT-Ship\burp_extension\pyritship\build\libs\
```

with content: `pyritship-0.0.1.jar`


## 🧩 Install the Extension in Burp Suite

1. Open **Burp Suite**
2. Go to the **Extender** tab
3. Click **Extensions** → **Add**
4. Extension type: **Java**
5. Select the `.jar` file you just built

✅ If successful, you’ll see a new tab called **PyRIT-Ship** inside Burp Suite.

